<%= form_for(@petsshelter) do |f| %>
  <% if @petsshelter.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@petsselter.errors.count, "error") %> prohibited this pet from being saved:</h2>

      <ul>
      <% @petsshelter.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  
  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  
 <% end %>