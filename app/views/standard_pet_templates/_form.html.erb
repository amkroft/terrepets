<%= form_for(@standard_pet_template) do |f| %>
  <% if @standard_pet_template.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@standard_pet_template.errors.count, "error") %> prohibited this standard_pet_template from being saved:</h2>

      <ul>
      <% @standard_pet_template.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.file_field :uploaded_file, accept: 'image/png,image/gif,image/jpeg' %>
  </div>
  <div class="field">
    <%= f.label :rights %><br>
    <%= f.text_area :rights %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
